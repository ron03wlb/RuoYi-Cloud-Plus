/*
 * ===========================================
 * ruoyi-common-excel
 * Excel 导入导出模块
 * ===========================================
 */

description = "ruoyi-common-excel"

dependencies {
    // ===========================================
    // 项目内依赖
    // ===========================================
    api(project(":ruoyi-common:ruoyi-common-json"))

    // ===========================================
    // FastExcel（Excel 处理库）
    // ===========================================
    api(libs.fastexcel)
}
