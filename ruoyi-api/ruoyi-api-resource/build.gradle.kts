/*
 * ===========================================
 * ruoyi-api-resource
 * 资源服务接口模块
 * ===========================================
 */

description = "ruoyi-api-resource 资源服务接口模块"

dependencies {
    // ===========================================
    // 项目内依赖
    // ===========================================

    // 核心模块
    api(project(":ruoyi-common:ruoyi-common-core"))
}
